name: Cross-Platform Build Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test_build:
    name: Build & Test on ${{ matrix.os }} / Py ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.12", "3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest wcwidth Pillow pyperclip

      - name: Set up MSVC Developer Command Prompt
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build and Install RC-TUI (Unix)
        if: runner.os != 'Windows'
        run: pip install -e .

      - name: Build and Install RC-TUI (Windows)
        if: runner.os == 'Windows'
        run: pip install .

      - name: Smoke Test (Import)
        run: |
          python -c "import rc_tui; print('RC-TUI version:', rc_tui.__version__)"

      - name: Run Unit Tests
        run: pytest tests/ -q

  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ruff
      - run: ruff check src/python/rc_tui tests
      - run: ruff format --check src/python/rc_tui tests

  build_wheels:
    name: Build wheel & smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install build
      - run: python -m build --wheel
      - uses: actions/upload-artifact@v4
        with:
          name: wheel
          path: dist/*.whl
      - run: pip install dist/*.whl pytest wcwidth Pillow pyperclip
      - run: python -c "import rc_tui; print(rc_tui.__version__)"
      - run: pytest tests/ -q
